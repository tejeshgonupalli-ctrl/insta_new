# Instagram Automation System

Features:
- Auto download reels
- Filter + watermark videos
- Auto content generation using AI (caption, hashtags, hooks)
- Upload scheduler with retry logic
- Streamlit UI dashboard

Run locally:
1. py -3.10 venv venv
2. venv\Script\activate
3. pip install -r requirements.txt
4. streamlit run app.py
